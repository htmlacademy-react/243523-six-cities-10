export enum AppRoute {
  Login = '/login',
  Root = '/',
  Room = '/offer',
  Favorites = '/favorites'
}

export enum AuthorisationStatus {
  Auth = 'Auth',
  NotAuth = 'NoAuth',
  UnKnown = 'UnKnown'
}
